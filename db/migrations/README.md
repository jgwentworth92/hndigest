# Database Migrations

SQL migration files executed in order on startup. Each file creates or alters tables in the SQLite database.

Naming convention: `001_initial_schema.sql`, `002_add_index.sql`, etc.
